import { Model } from 'sequelize-typescript';
import { PodcastEpisode } from './podcast-episode.model';
export declare class Podcast extends Model<Podcast> {
    id: string;
    title: string;
    titleIgnorePrefix: string;
    author: string;
    releaseDate: string;
    feedURL: string;
    imageURL: string;
    description: string;
    itunesPageURL: string;
    itunesId: string;
    itunesArtistId: string;
    language: string;
    podcastType: string;
    explicit: boolean;
    autoDownloadEpisodes: boolean;
    autoDownloadSchedule: string;
    lastEpisodeCheck: Date;
    maxEpisodesToKeep: number;
    maxNewEpisodesToDownload: number;
    coverPath: string;
    tags: string[];
    genres: string[];
    numEpisodes: number;
    podcastEpisodes: PodcastEpisode[];
    static getTitleIgnorePrefix(title: string): string;
    static getTitlePrefixAtEnd(title: string): string;
    get hasMediaFiles(): boolean;
    get hasAudioTracks(): boolean;
    get size(): number;
    checkCanDirectPlay(supportedMimeTypes: string[], episodeId: string): boolean;
    getTracklist(libraryItemId: string, episodeId: string): any[];
    getChapters(episodeId: string): any[];
    getPlaybackTitle(episodeId: string): string;
    getPlaybackAuthor(): string;
    getPlaybackDuration(episodeId: string): number;
    getLatestEpisodePublishedAt(): number;
    checkHasEpisodeByFeedEpisode(feedEpisode: any): boolean;
    toOldJSON(libraryItemId: string): any;
    toOldJSONMinified(): any;
    toOldJSONExpanded(libraryItemId: string): any;
}
